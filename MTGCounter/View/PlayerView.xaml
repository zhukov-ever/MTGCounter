<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="MTGCounter.View.PlayerView">

    <ContentView.Content>

        <Grid BackgroundColor="White" Padding="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Label Text="Player" HorizontalOptions="Center" Font="18" 
                   Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" />
            <Button Text="-" BackgroundColor="Silver" Font="20"
                    Grid.Row="1" Grid.Column="0" />
            <Label Text="30" VerticalTextAlignment="Center" Font="28"
                   HorizontalTextAlignment="Center"
                   Grid.Row="1" Grid.Column="1" />
            <Button Text="+" BackgroundColor="Silver" Font="20"
                    Grid.Row="1" Grid.Column="2" />
        </Grid>

	</ContentView.Content>
    
</ContentView>
